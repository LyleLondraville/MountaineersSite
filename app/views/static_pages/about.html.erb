<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner" role="listbox">
    <%slides = YAML.load(File.read "#{Rails.root}/app/views/static_pages/data/homepage_carousel.yml" ) %>
    <%count = 1 %>
    <%slides.each do |slide| %>
      <%if count==1 %>
        <div class="carousel-item active" style="background-image: url('<%= asset_path slide["photo"]%>')">
          <div class="carousel-caption d-none d-md-block">
            <h3 class="display-4"><%= slide["description"]%></h3>
            <%#rails <p class="lead">This is a description for the first slide.</p>%>
          </div>
        </div>
        <%count+=1 %>
      <%else %>
        <div class="carousel-item active" style="background-image: url('<%= asset_path slide["photo"]%>')">
          <div class="carousel-caption d-none d-md-block">
            <h3 class="display-4"><%= slide["description"]%></h3>
            <%#rails <p class="lead">This is a description for the first slide.</p>%>
          </div>
        </div>
      <%end %>

    <% end %>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
